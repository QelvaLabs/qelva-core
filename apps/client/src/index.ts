// Placeholder for Qelva web client (dashboard, personas, intents, etc.)

export function startQelvaClient() {
  // This will later boot a real web app (Next.js/React).
  // For now it's just a placeholder.
  console.log('Qelva client placeholder');
}
